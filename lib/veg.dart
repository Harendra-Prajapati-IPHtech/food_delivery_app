import 'package:flutter/material.dart';

class Veg extends StatelessWidget {
  const Veg({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('this is veg page'),
    );
  }
}
