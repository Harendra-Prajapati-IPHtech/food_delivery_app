import 'package:flutter/material.dart';

class NonVeg extends StatelessWidget {
  const NonVeg({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('this is nonveg page'),
    );
  }
}
